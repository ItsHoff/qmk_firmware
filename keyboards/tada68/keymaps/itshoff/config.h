#pragma once

#include "../../config.h"

/* Allows quick tap followed by hold modifier (like esc + caps layer) but break tapping toggles.
 * https://docs.qmk.fm/#/feature_advanced_keycodes?id=tapping-force-hold
 */
#define TAPPING_FORCE_HOLD
